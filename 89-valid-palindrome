public Boolean isPalindrome(String str){
    String regExp = '[\\W_]';
    String newString = str.toLowerCase().replaceAll(regExp, '');
    // Declare and initialize the two pointers
    Integer left = 0;
    Integer right = str.size() - 1;
    while (left < right) {
        if (str[left] != str[right]) {
            return false;
        }
        left++;
    }
    return true;


    // if (newString.reverse() == newString) {
    //     isPalindrome = true;
    //     return isPalindrome;
    // }
    // return isPalindrome;
}